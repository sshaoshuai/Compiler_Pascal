prog SSS( first , second);
var
	a , b : array [1..100] of integer ;
	f : real;

function work (a1 , b1 : array [1..100] of real; x : integer ) : real ;
begin
	x := 100
end;

procedure dfs;
begin
	if aa < bb then
	begin
		while a2 < b2 do
		begin
			work(a2, a2 , x2);
			a2 := b2 * 123
		end
	end
	else
		work( a3 , b3 , x3 )
end ;

begin
	a1 := ( a2 + b2 ) * x + x3 ;
	work( a1 , a2) ;
	if a1 < a2 then work( a1, a2) else work( a2, a1) ;
	while a1 < a2 do
	begin
		sss := jl ;
		dfs(xxx)
	end
end